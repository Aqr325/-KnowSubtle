# WordCosmos Learning Universe · 用户手册（运维与验证）

> 项目总览见 `README.md`。本手册面向**维护者**：讲清楚怎么跑、数据存哪、怎么验证每个功能、打包后怎么冒烟，以及历史修复记录。
> 系统版本：**v3.0.0**（存储层已从 JSON 文件迁移到 SQLite）。

---

## 一、快速开始

### 1.1 源码运行（DEMO 模式，无需 API Key）

```bash
# 项目根目录
METAGPT_STUBBED=1 python app.py
# 浏览器打开 http://127.0.0.1:8000
```

DEMO 模式由 `local_metagpt` 桩驱动：在导入业务模块前注入假 `metagpt` 包，
orchestrator 跳过真实大模型调用，改走离线占位逻辑。整个后端可**完全离线**运行。

### 1.2 环境变量

| 变量 | 作用 | 默认 |
|------|------|------|
| `METAGPT_STUBBED` | 设为 `1` 进入 DEMO（离线）模式 | 未设（尝试真实 metagpt） |
| `LAS_DATA_DIR` | 重定向 SQLite 数据目录（源码模式可用） | 未设时用 `.learning_memory/` |
| `LAS_DB_PATH` | 直接指定数据库文件路径 | 未设 |
| `APPDATA` | 打包 exe 在 Windows 的数据根（含 `WordCosmos/Data`） | 系统 AppData |

> ⚠️ 注意：在某些受限/沙箱环境中，含路径的 `LAS_DATA_DIR` 可能不生效。
> 验证时最稳妥的做法是**先 `rm -rf .learning_memory` 再启动**，保证全新单实例 DB。

### 1.3 打包运行（分发给用户）

产物：`Release-Package/Core/main/main.exe`（含 `_internal/` 依赖）。
双击或经 `launcher.py` 启动，单实例守护 + 端口冲突探测 + 原生 WebView 窗口。

---

## 二、数据存储（SQLite v3）

- ORM：`learning_agent_system/database/`（SQLAlchemy 2.0 + aiosqlite，WAL 模式）。
- 共 **21 张表**；`Session.metadata_json` 以 JSON blob 存完整 `SessionContext`（最小变更、向后兼容）。
- 首次启动自动把旧 JSON 数据（`vocab` / `daily_goals` / `stats_history` / `checkpoint_*.json`）迁移进库。
- 端点（Stats / Vocab / Goals / Sessions / Exercises）全部走 Repository 模式，**API 契约不变**，前端零改动。

数据目录位置（按优先级）：
1. `LAS_DB_PATH` 直接指定
2. `LAS_DATA_DIR` 指定目录下的 `wordcosmos.db`
3. 否则 `.learning_memory/wordcosmos.db`（源码）或 `%APPDATA%/WordCosmos/Data/wordcosmos.db`（打包）

---

## 三、功能验证：verify_all.py

`verify_all.py` 是标准库 `urllib` 实现的端到端验证脚本（**无第三方依赖**），
覆盖全部 API 端点 + 完整 AI 流水线 + DB CRUD，共 **54 项**。服务需先启动。

```bash
# 终端 1：启动服务
METAGPT_STUBBED=1 python app.py

# 终端 2：运行验证（默认打 http://127.0.0.1:8000）
python verify_all.py
# 或指定基址
python verify_all.py http://127.0.0.1:8001
```

退出码：`0` = 全部通过；`1` = 有失败项（并打印失败清单）。

### 验证覆盖范围（54 项分组）

| 阶段 | 覆盖内容 | 项数 |
|------|----------|------|
| A 默认/演示端点 | health、version、无会话时的 planets/stats/goals/tasks/achievements/knowledge-graph/report-card 静态分支 | ~10 |
| B 词库 CRUD | 按学科 GET、POST 新增、PUT 标记掌握、DELETE、subjects 列表、去重 | ~14 |
| C 目标 CRUD | goals/today 读取、POST 进度累加、目标设定、复习 GET/到期、统计汇总 | ~12 |
| D 完整 AI 流水线 | 建会话→轮询 completed→会话 LIST/DETAIL/STATUS；依赖会话的 planets/progress/tasks+toggle/achievements/knowledge-graph/tutor/chat/agent/exercises/report-card | ~18 |

> 最近一次干净单实例复验：**54/54 PASS，0 失败**。

---

## 四、打包后验证（headless 端到端冒烟）⚠️ 关键

> **教训**：pytest 覆盖不到 PyInstaller 的 `frozen` 分支。曾经 `repo.py` 缺 `import sys`，
> 测试全绿，但打包后的 exe 一启动就崩——必须拉起**真实 exe** 冒烟才能发现。

步骤（沙箱拦截 `AppData` 写入，用 `APPDATA` 重定向绕过）：

```bash
# 清理残留，避免多实例/脏数据假象
netstat -ano | grep ':8000'        # 找到占用 PID
taskkill /F /PID <占用PID>

# 用 APPDATA 重定向到项目内可写目录，headless 拉起 exe
rm -rf _exetest_apdata
APPDATA="$PWD/_exetest_apdata" WC_HEADLESS=1 Release-Package/Core/main/main.exe > exe.log 2>&1 &
for i in $(seq 1 45); do
  curl -s http://127.0.0.1:8000/api/health && break; sleep 1
done

# 同一套 54 项直击打包二进制
python verify_all.py
```

确认：health 返回 200、无启动崩溃，且 `verify_all.py` 仍 **54/54 PASS**。

---

## 五、已知修复记录（v3 验证阶段）

| # | 现象 | 根因 → 修复 | 提交 |
|---|------|-------------|------|
| 1 | 前端“标记掌握”调 `PUT /api/vocab/{id}` 仅传 `{mastered}` → **422** | `VocabEntry` 要求 `word` 必填 → 新增全可选 `VocabUpdateEntry` 模型 | `b25364ab` |
| 2 | `GET /api/report-card` → **500** `'LearningGoal' has no attribute 'created_at'` | schema 缺字段 → `LearningGoal` 补 `created_at` | `b25364ab` |
| 3 | demo 下 `tasks toggle` → **404**；report-card 又报 `'LearningPath' has no attribute 'estimated_hours'` | demo 上下文 `LearningPath` 没填 `modules` → orchestrator 填 3 个 `LearningModule`；`total_hours` 改从 `modules` 求和 | `b25364ab` |
| 4 | 打包 exe 启动即崩（frozen 分支 `NameError: name 'sys' is not defined`） | `repo.py` 的 `migrate_json_to_db` 用 `sys` 但未 `import sys` → 补 `import sys` | `d66c5982` |

> 第 4 条是“会带病发布”的典型：pytest 走非 frozen 分支，完全漏掉，靠 headless 拉起 exe 才抓到。

---

## 六、CI（GitHub Actions）

`.github/workflows/verify.yml`：推送 `main` 或开 PR 时自动执行——

1. 安装轻量运行时依赖（**不装真实 metagpt**，靠桩离线跑）。
2. `METAGPT_STUBBED=1` 后台启动服务，轮询 `/api/health` 直到就绪。
3. 运行 `verify_all.py`；**任一功能失败则 CI 标红**，阻断合入。

> 说明：CI 跑的是**源码级**验证（覆盖上述 3 个 bug 及回归）。
> 打包 exe 的 headless 端到端冒烟为 Windows 专属步骤，当前作为**发布前手动检查**（见第四节）。
